name: Hello World!

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Greet
        run: |
          echo "Hello World!"
      - run: echo "Hello World! again..."
      - run: date
      - run: ls -l
